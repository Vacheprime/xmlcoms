package stream_elements

// Used to start and end xml streams
const (
    OpenStreamTag string = "<stream>"
    OpenStreamName string = "stream"
    CloseStreamTag string = "</stream>"
    CloseStreamName string = OpenStreamName
)
